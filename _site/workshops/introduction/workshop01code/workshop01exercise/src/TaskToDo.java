

import java.util.Random;

public class TaskToDo {
	/**
	 * 
	 * @param arr
	 * @return sum of all items in the array. for example, 
	 * if arr = {10, 70, 20}, method should return 100 (10+70+20)
	 */
	public static int sum(int[] arr) {
		return 0; //to be completed
	}

	/**
	 * 
	 * @param data
	 * @return sum of the even numbers in the array. for example, 
	 * if data = {8, 13, -6, 15, -19, 0, 12}, method should return 14 (8 + -6  + 0 + 12)
	 * 
	 */
	public static int sumEven(int[] data) {
		return 0; //to be completed
	}
	
	/**
	 * 
	 * @param a
	 * @param item
	 * @return number of times item exists in arr
	 */
	public static int count(int[] arr, int item) {
		return 0; //to be completed
	}

	/**
	 * 
	 * @param items
	 * @param target
	 * @return true if the array contains target, false otherwise. for example, 
	 * if items = {10, 70, 20} and target = 70, method should return true. 
	 * However, if items = {10, 70, 20} and target = 80, method should return false. 
	 */
	public static boolean contains(int[] items, int target) {
		return false; //to be completed
	}

	//advanced
	/**
	 * 
	 * @param arr
	 * @return number of items that occur exactly once in the array. for example,
	 * if arr = {1, 8, 3, 5, 2, 7}, method should return 6.
	 * if arr = {1, 8, 3, 5, 2, 8, 1, 8, 7}, method should return 4.
	 */
	public static int countUnique(int[] arr) {
		return 0; //to be completed
	}	

	public static void main(String[] args) {
		//DO NOT MODIFY ANY PART OF MAIN METHOD
		int[] arr = {3,1,4,2,5,9,2,6,5}; //math enthusiasts must resist the urge to kill me
		System.out.println(sum(arr)); //should be 37
		System.out.println(sumEven(arr)); //should be 18
		System.out.println(contains(arr, 8)); //should be false
		System.out.println(contains(arr, 9)); //should be true
		System.out.println(countUnique(arr)); //should be 5

		//for supplementary task
		Random r = new Random();

		/*
		 * a random integer between low and high is generated by
		 * int rand = low + r.nextInt(high - low + 1);
		 */

		//WRITE YOUR CODE FOR SUPPLEMENTARY TASK HERE
	}
}
