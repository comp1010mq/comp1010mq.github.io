

/**
 * email address is stored as a combination of username and domain
 * for example, username = "gaurav.gupta"
 * domain = "mq.edu.au"
 * thus the email address is "gaurav.gupta@mq.edu.au"
 * @author gauravgupta
 *
 */
public class EmailAddress {
	private String username, domain;
	
	/*
	 * add getters and setters. for now, we won't 
	 * do any validation on the values passed 
	 * (simply copy into instance variables)
	 */
	
	/*
	 * add a parameterized constructor with values 
	 * passed for both instance variables
	 */
	
	/*
	 * add a default constructor with no parameters, 
	 * setting username to "tba" and domain to "gmail.com"
	 */
	
	/*
	 * add a toString method that returns email address 
	 * as described in the class header comment
	 */
	
	/*
	 * 
	 * add an equals(EmailAddress) method based on 
	 * following requirements,
	 * 
	 * @param other
	 * @return
	 * true if the calling object and the parameter object 
	 * refer to the exact same email address, false otherwise
	 */
}
