//a simple processing code

line(0, 0, width, height);
line(width, 0, 0, height);

//coordinate for all subsequent text is center aligned (in both x and y directions)
textAlign(CENTER, CENTER);

text("Welcome!", width/2, height/2); 
//message in center of display window

